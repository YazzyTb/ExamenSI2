services:
  - type: web
    name: angular-ecommerce-ssr
    runtime: node
    buildCommand: npm install && npm run build:ssr
    startCommand: node server.js
    env: node
    plan: free
    autoDeploy: true
