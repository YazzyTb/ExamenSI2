services:
  - type: web
    name: angular-ecommerce-ssr
    runtime: node
    buildCommand: npm install && npm run build:ssr
    startCommand: npm start
    env: node
    plan: free
    autoDeploy: true
